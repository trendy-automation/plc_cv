to deploy:
  on pc-cv:
    sudo echo 'PermitRootLogin prohibit-password' >> /etc/ssh/sshd_config
    sudo echo 'PasswordAuthentication yes' >> /etc/ssh/sshd_config

  on master:
    cd plc_cv
    docker build ($pwd)/docker-compose -t plc-cv-deploy
    docker run --rm -it -v ($pwd)/install/ansible/playbooks:/ansible/playbooks plc-cv-deploy install/plc-cv-playbook.yaml --extra-vars "ansible_sudo_pass=3470"
    # to check user user
    http://192.168.1.12:8080/

to test:
  on master:
    cd plc_cv
    docker build ($pwd)/jupyter -t jupyter-notebook
    docker run -it --rm -p 10000:8888 -v ($pwd)/jupyter:/home/jovyan/work jupyter-notebook